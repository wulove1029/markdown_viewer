# Changelog

## [Unreleased]

## [1.29.0] - 2026-08-27

### Added
- Markdown 編輯拆成兩條清楚入口：`Ctrl+E`／鉛筆固定使用原始 Markdown，`Ctrl+Shift+E` 使用原始碼並排預覽，`Ctrl+Shift+W`／圖層按鈕固定使用 Office 視覺編輯器；兩種模式都只讀寫標準 `.md` 純文字。
- 新建 Markdown 筆記可直接選擇原始 Markdown 或 Office 視覺編輯；工具列與 Markdown 分頁上的彩色 `MD`／`Office` 小標籤會同步顯示目前模式，背景分頁也能直接辨認。
- Office 模式開啟 front matter、wiki-links、Obsidian callouts 或帶標題的參照式連結前會顯示相容性提醒，取消時不建立編輯草稿、不更動檔案。

### Changed
- 安全預設改回原始 Markdown；檢視模式雙擊預設維持原本的文字選取／區塊操作，需要時可在偏好設定明確改成直接進入 Office 編輯。
- Office 切換至原始 Markdown 前會等待最新 WebEngine 快照與確認，避免快速切換時遺失最後一次輸入。

### Fixed
- 修正 Office 視覺編輯的縮放延遲：`Ctrl+滾輪` 不再受內建 250ms 節流而遺失大部分事件，現在會合併高解析度滾輪輸入並統一使用內容縮放；`Ctrl+=`／`Ctrl+-` 改用較快且可預期的標準級距，背景預覽與偏好設定則在操作停止後才同步一次，避免每一階都重複排版。

## [1.28.0] - 2026-08-26

### Added
- WYSIWYG 核心升級為 **Markdown Editor - Office Viewer 4.2** 的固定離線 fork，完整帶入其編輯器／程式碼主題、語系、KaTeX 與必要渲染資源，不依賴網路 CDN。
- 新增各文件獨立的 WYSIWYG session：一般文字及虛擬化長程式碼區塊都會分別恢復游標、選取範圍與捲動位置。

### Changed
- WYSIWYG 畫面與互動對齊 Office Viewer：固定且舒適的單列上方工具列、左側巢狀大綱、原生段落把手，以及程式碼語言／配色／複製／刪除選單；內容填滿視窗寬度並跟隨檢視縮放（`Ctrl+=`／`Ctrl+-`）與深淺色主題。`Esc` 逐層關閉目前選單／浮層，沒有開啟浮層時留在編輯器。
- 大文件輸入改以 UTF-16 小範圍增量同步到 Qt 草稿，不再每次按鍵重建整份 Markdown；儲存、匯出、切換分頁、改名與關閉改用非同步最終快照，避免卡頓與 debounce 尚未送出時遺失最後輸入。

### Fixed
- 修正檢視模式在存在未儲存的 WYSIWYG 編輯時，就地編輯、表格編輯與任務核取方塊仍可直接寫入磁碟而繞過未儲存內容的問題；現在會提示先進入編輯模式。
- 修正首次進入 WYSIWYG 先顯示空白／Auto 主題再重繪的閃動；最初一幀即使用正確文件、相對資源基準與主題。
- 修正多文件共用 WebEngine 時互相污染游標的問題；一般文字與虛擬化長程式碼區塊會各自恢復選取位置與捲動位置。

### Known limitations
- WYSIWYG 模式不解析 wiki-links 與 Obsidian callouts（顯示為一般文字），Mermaid 區塊顯示為程式碼；此類內容建議切回並排編輯，檢視模式渲染不受影響。

## [1.27.0] - 2026-08-26

### Added
- 新增所見即所得（WYSIWYG）Markdown 編輯器（內嵌離線 Vditor 3.11.3，含中文介面與 KaTeX 數學）：在檢視模式的文件上雙擊即可直接開始編輯，表格、標題、清單、程式碼區塊皆以成品樣式呈現與修改；`Esc` 返回檢視、`Ctrl+S` 儲存，未儲存的編輯在返回檢視與切換分頁後完整保留。編輯模式亦可用 `Ctrl+Shift+W` 在 WYSIWYG 與原始碼並排編輯之間切換，各分頁記住自己的選擇。
- WYSIWYG 工具列提供大綱、復原／重做、標題、粗斜體、刪除線、連結、清單、核取清單、表格（`Ctrl+M`，可視覺化增刪列欄）、引用、程式碼區塊，並新增存檔、匯出 PDF／Word／HTML、插入圖片與深淺色切換按鈕；所有按鈕滑鼠停留皆顯示中文說明。
- WYSIWYG 右鍵選單：複製、貼上、貼上為純文字、匯出 PDF／Word／HTML、插入圖片、在資料夾中顯示；編輯中匯出一律使用畫面上的最新內容（未儲存也是最新版），檔案選單並新增「匯出 HTML…」。
- 段落左側新增 Notion 式把手：hover 浮現「＋」（插入新段落）與「⋮⋮」（拖曳搬移區塊，附插入位置指示線與頁緣自動捲動），拖放結果即時同步回 Markdown。

### Changed
- 進入編輯模式（雙擊、`Ctrl+E`、工具列鉛筆、新建筆記）預設直達 WYSIWYG；「設定 → 行為」可改回原始碼並排編輯，亦可將檢視雙擊行為改回原本的就地區塊編輯。純文字（`.txt`）維持原本的純文字編輯。
- WYSIWYG 內容改為填滿視窗寬度，並跟隨檢視縮放（`Ctrl+=`／`Ctrl+-`）與深淺色主題。

### Fixed
- 修正檢視模式在存在未儲存的 WYSIWYG 編輯時，就地編輯、表格編輯與任務核取方塊仍可直接寫入磁碟而繞過未儲存內容的問題；現在會提示先進入編輯模式。
- 修正自 WYSIWYG 按 `Esc` 返回檢視後，切換分頁再切回會誤回編輯器、以及視窗標題遺漏未儲存標記的問題。

### Known limitations
- WYSIWYG 模式不解析 wiki-links 與 Obsidian callouts（顯示為一般文字），Mermaid 區塊顯示為程式碼；此類內容建議切回並排編輯，檢視模式渲染不受影響。
- 以把手拖曳搬移區塊的動作無法用 `Ctrl+Z` 復原（可再拖回原位）。

## [1.26.0] - 2026-08-25

### Added
- 新增「新增筆記」流程，可直接建立 `.md` 或 `.txt`、指定建立位置與初始內容；純文字檔正式支援開啟、最近清單、文件庫、分頁、搜尋、編輯與 session restore，並保留 UTF-8 BOM、UTF-16、Big5 及 LF／CRLF。
- Markdown 編輯器新增行號、格式選單與工具列、選取文字浮動工具列、`/` 快速指令、智慧括號與清單延續；涵蓋標題、粗斜體、清單、連結、表格、程式碼、Mermaid、數學式、wikilink 與醒目標示。Mermaid 插入會先提供可直接預覽與改字的最小流程圖範本。
- 新增各分頁獨立編輯工作區，每份文件各自保留未儲存內容、Undo／Redo、游標、選取範圍、捲動位置與編輯／並排模式；切換分頁不再要求先儲存。
- 新增編輯狀態列，持續顯示行列、字數、Markdown／純文字、編碼及換行格式。
- 新增自動復原草稿與復原對話框；異常關閉、原檔被刪除或下次啟動時，都能保留並恢復尚未儲存的內容。
- 新增範本、圖片、附件與最近資源插入；支援拖放及貼上圖片、安全的特殊字元連結，以及高風險附件開啟保護。

### Changed
- 新建 Markdown 筆記會直接進入並排即時預覽，新建 TXT 則直接進入純文字編輯；「編輯」選單集中原生 Undo、剪貼與搜尋，「格式」選單集中 Markdown 語法與資源操作。
- 多分頁關閉與程式結束流程可一次儲存全部草稿，並在個別分頁保留清楚的未儲存標記。

### Fixed
- 修正切到預覽後關閉背景編輯分頁時，Qt 仍持有已刪除 `QTextDocument` 而可能造成原生閃退的問題。
- 修正背景乾淨分頁被外部修改或刪除時，重新切回可能顯示舊內容或讓儲存覆寫較新磁碟版本的問題；原檔刪除時會把記憶體內容提升為可復原草稿。
- 修正部分程式化文字替換未設為 modified，造成 TXT 儲存看似成功卻未寫入的問題；UTF-8 BOM、UTF-16 與 CRLF round-trip 也一併加入回歸保護。

## [1.25.0] - 2026-08-25

### Changed
- 文件庫的資料夾樹改用清楚的展開箭頭與淡色階層導引線，文件庫、收合資料夾、展開資料夾也有不同圖示；同時加寬每層縮排並縮短列距，深層目錄更容易辨認，也能在側欄中一次看到更多檔案。
- 目錄改用真正的標題層級縮排與淡色導引線，列距更緊湊，目前章節會顯示左側強調線；長標題以省略號顯示並保留完整提示，不再出現水平捲軸。同時修正 PDF 大綱第一頁無法點擊跳轉的問題。

## [1.24.0] - 2026-08-24

### Added
- 閱讀模式點三下表格，改以表格網格編輯器就地修改：直接在格子裡打字，用工具列或列／欄旁的按鈕新增、刪除列與欄，並可設定每一欄靠左、置中或靠右；Tab 移到下一格、Enter 移到下一列，從試算表複製的多欄多列資料可整片貼入。`Ctrl+Enter` 儲存、`Esc` 取消，需要時仍可切回原始碼編輯，管線符號與對齊列不再需要手動對齊。

### Changed
- 閱讀模式的就地編輯（表格網格與原始碼兩種）在檔案被其他程式改動時，不再自動重新載入預覽把你打的內容一起沖掉；改為原地留下編輯器與一條警告列，附「重新載入預覽」按鈕，等你把內容複製走再自己按。
- 預覽中開著編輯器時，按重新載入、切換到編輯／分割模式，或收到檔案已在外部變更的提示，都會先問你要不要捨棄那些編輯，不再無聲丟棄。
- 就地編輯的寫回改為同時比對檔案版本與區塊內容。文件裡有兩張內容完全相同的表格時，外部改動不會再讓編輯落到另一張表上。

### Fixed
- 表格網格編輯器改為佔滿原表格的寬度，欄位不再被擠成細長條、長內容不再折成好幾行；每一欄的對齊與增刪按鈕也正確地站在自己那一欄上方。
- 開啟就地編輯時會收起標註的顏色工具列，不再浮在編輯器上面。
- 修正表格網格編輯器開啟後整片空白看不見的問題。文件主題會把頁面上每個表格改成區塊排版並左右置中（寬表格橫向捲動用的手法），網格本身也是表格因而被一起套用，導致寬度算成 0 並被推離可見範圍。
- 修正匯出 PDF 選擇「單一長頁（不分頁）」後，仍依視窗高度輸出成多個實體頁面的問題；現在會依完整文件高度建立單頁，超長文件也不再自動降回 A4，若無法取得可靠尺寸則停止匯出並提示重試。

## [1.23.0] - 2026-08-18

### Changed
- 文件庫檔案樹改在背景執行緒掃描資料夾，主視窗與文件內容不再等整個文件庫掃完才出現；掃描期間狀態列顯示「正在掃描文件庫…」，樹掃完後自動填入並套用上次的展開／選取狀態。
- 檔案樹掃描改用 `os.scandir` 直接取用目錄列表中的檔案類型，不再對每個項目個別 `stat`，大型文件庫（數千個資料夾、USB 或冷快取磁碟）掃描時間縮短一個數量級；資料夾／檔案圖示改為快取共用。
- PyMuPDF 改為第一次需要 PDF 大綱時才載入，縮短冷啟動的模組載入時間。

### Fixed
- 修正第一次開啟程式（尤其文件庫在 USB 碟）時，要等好幾秒才出現視窗與內容的問題。

## [1.22.0] - 2026-08-10

### Added
- 選取文字後按右鍵可「翻譯選取內容」，閱讀模式、PDF 檢視與編輯器皆支援；譯文顯示在可複製的非模態視窗，翻譯期間不阻塞介面。
- 偏好設定新增「翻譯」分頁：可選 MyMemory（預設，免註冊）、Google 翻譯（免註冊）或 DeepL（需 API 金鑰），並可設定目標語言（繁體中文／简体中文／English／日本語）。
- 譯文視窗可直接切換翻譯服務與目標語言，切換後立即重譯並記住選擇，不必進偏好設定；另有「重新翻譯」可忽略快取重新取得譯文。
- 譯文快取（最多 256 筆，LRU）：重看同一段落直接取用先前結果，不再重複消耗免費額度，狀態列會標示「快取」。

### Fixed
- PDF 檢視現在支援點兩下選取整個字詞、點三下選取整行，與閱讀模式的行為一致；中文會選取整串連續字元而非單一字。原本 PDF 只能拖曳選取。

## [1.21.0] - 2026-08-07

### Added
- PDF 支援 `Ctrl+滑鼠滾輪`，可依游標位置連續放大／縮小，並沿用與記住內容縮放比例。

### Changed
- PDF 縮放改為先拉伸最近的頁面預覽，停止操作後再以背景多執行緒精確重繪；大型與高倍率頁面只分塊渲染可見區域並預載周邊，降低操作期間的介面停頓。
- PDF 大綱改在首個可見頁面顯示後於背景載入，不再阻塞文件開啟；快速切檔與重新載入時，過期的大綱與渲染結果不會套用到目前文件。
- 檔案樹切換主題時改為原地更新圖示與文字色彩，保留既有選取與展開狀態，不再為換色重建整棵樹。

### Fixed
- 修正大型文件庫啟動時因重複套用主題而同步掃描三次的效能退化；主視窗建構期間現在只掃描一次。
- 修正 PDF 連續縮放時每個滾輪封包都重新排版與渲染，造成明顯卡頓的問題。
- 修正啟動後第一次開啟 PDF 時，可能未套用已儲存內容縮放比例的問題。

## [1.20.1] - 2026-07-30

### Changed
- 閱讀模式原地編輯改為「點三下」觸發：點兩下保留給瀏覽器原生的選字功能，快速選取複製文字不會再誤開編輯框。
- 「關於」對話框新增「本版更新」摘要，顯示目前版本的修正內容。

### Fixed
- 修正原地編輯儲存後畫面先跳回頁面頂端、下一步才回到編輯位置的閃爍問題；重新整理與外部改檔自動重載也一併不再閃頂。

## [1.20.0] - 2026-07-28

### Added
- 編輯模式與並排模式支援直接貼上剪貼簿圖片，圖片會自動存入文件旁的 `assets/` 資料夾並插入 Markdown 連結；也可將圖片檔拖入編輯器，自動複製或建立相對連結。
- 閱讀模式支援雙擊段落、標題、清單或程式碼區塊原地編輯 Markdown；可用 `Ctrl+Enter` 儲存、`Esc` 取消，編輯框內亦可直接貼上圖片。

### Improved
- 文件原子寫入遇到 Windows 防毒、同步工具或檔案監看造成的短暫鎖定時，會在極短時間內有限重試，降低編輯、待辦勾選與行內編輯偶發儲存失敗的機率。

## [1.19.0] - 2026-07-14

### Added
- 檔案「加入標籤…」：在「檔案」分頁對檔案（可多選）右鍵「加入標籤…」，會彈出下拉欄位，可選擇現有標籤或直接輸入新標籤，一次加到所選檔案。

### Improved
- 標籤操作大幅加速：加標籤／改標籤時只更新受影響的檔案列，不再每次重新掃描整個文件庫，資料夾很大時也不再卡頓。
- 標籤／標註的 sidecar 檔（`<檔名>.md.notes.json` 等）改為隱藏檔，不再在檔案總管中形成雜訊（仍存放於檔案旁、隨檔案一起搬移）；既有的 sidecar 會在下次開啟文件庫時一併隱藏。
- 下拉選單（例如「加入標籤」）現在顯示清楚的下拉箭頭，不會再被誤認為純輸入框。
- 「標籤」分頁定位為瀏覽已分類的檔案，移除頁首的「＋ 新增標籤」按鈕；建立新標籤改由檔案的「加入標籤」或「管理標籤」進行。

### Fixed
- 不再為「沒有任何標籤與標註」的檔案產生空的 `.notes.json` 檔；既有的空 sidecar 會在清空時一併刪除。

## [1.18.0] - 2026-07-14

### Added
- PDF 檔案標籤：PDF 現在也能像 Markdown 一樣為整份文件加上標籤，Markdown 與 PDF 統一以標籤分類管理。
- 彩色標籤與檔案圖示：每個標籤可從 7 色調色盤指定顏色；「檔案」分頁在檔名下方以彩色具名膠囊顯示標籤，並為資料夾、Markdown、PDF 各加專屬圖示，一眼分辨資料夾與可開啟的檔案。
- 標籤瀏覽樹：「標籤」分頁改為可展開／收合的樹，點一個標籤即在其下列出帶該標籤的檔案（Markdown＋PDF），點檔案直接開啟。
- 管理標籤對話框：可勾選為目前檔案指派標籤、新建標籤（含選色），並可對每個既有標籤重新命名或刪除。
- 重新命名標籤：於標籤節點右鍵或管理標籤視窗改名，會同步套用到所有帶該標籤的檔案並保留顏色。
- 標籤分頁檔案右鍵：展開標籤後對檔案可開啟、管理標籤、重新命名、移動、刪除、在檔案總管顯示，與「檔案」分頁一致。

### Improved
- 標籤分類集中在「標籤」分頁：點選標籤只在該分頁展開對應檔案，不再連動篩掉「檔案」「最近」分頁的其他檔案。

### Fixed
- 修正在「標籤」分頁切換檔案時畫面會短暫彈出小視窗的問題。
- 修正亮色主題下選取帶標籤檔案時檔名文字不清楚、以及「管理標籤」對話框在暗色主題下文字看不見的問題。

## [1.17.0] - 2026-07-11

### Added
- 排除資料夾清單：偏好設定新增「排除資料夾」（一行一項，支援資料夾名稱與相對路徑片段），排除同時作用於檔案樹、全文搜尋、`[[` 補全與筆記關聯圖；內建預設新增 build、dist、Pods 等常見生成物目錄。

### Improved
- 「檔案」分頁現在會隱藏不含任何可開啟檔案（Markdown／PDF）的資料夾，加入整個程式碼專案資料夾時清單不再充滿雜訊；右鍵新建的空資料夾在本次開啟期間仍保持可見。

### Fixed
- 修正測試套件在無畫面環境下因存檔失敗對話框造成的間歇性停擺（產品行為不變）。

## [1.16.0] - 2026-07-11

### Added
- 全文搜尋：新增「搜尋」分頁與 `Ctrl+Shift+F`，可跨整個文件庫遞迴搜尋 Markdown 內文，結果依檔案分組並可點擊跳轉。
- 檔案樹管理：「檔案」分頁改為樹狀檢視，右鍵可新增筆記／資料夾、重新命名、移動、刪除（送資源回收桶）；重新命名與移動會同步搬移標註與標籤索引；並記憶上次展開狀態。
- 並排即時預覽：檢視模式改為「預覽／編輯／並排」三態（工具列按鈕循環，`Ctrl+Shift+E` 直接並排），並排時邊打字邊渲染並支援捲動同步。
- `[[` 連結自動補全：編輯時輸入 `[[` 會列出文件庫內的筆記名供選擇。
- 內文標籤：Markdown 內文的 `#tag` 會自動納入標籤索引（正確排除程式碼區塊與標題）。
- 筆記關聯圖：新增「檢視 → 筆記關聯圖」與 `Ctrl+G`，以力導向圖顯示 `[[連結]]` 關係，支援依資料夾分群著色、圖例顯示／隱藏、滑鼠懸停高亮鄰居、拖曳縮放與點擊開檔。
- 每日筆記與範本：`Ctrl+D` 開啟今日筆記（`YYYY-MM-DD.md`）；支援範本資料夾與 `{{date}}`／`{{time}}`／`{{title}}` 變數，可於編輯時插入範本；偏好設定新增相關選項。

### Improved
- 標籤篩選：點選標籤時，除了「最近」清單，「檔案」分頁的檔案樹也會一併篩選。

### Fixed
- 修正 `[[wikilink]]` 解析會誤抓程式碼區塊／行內程式碼中的範例文字，導致關聯圖出現多餘節點的問題。

## [1.15.0] - 2026-07-09

### Added
- PDF 螢光標記現在可以直接在畫布上右鍵刪除；螢光面板新增「刪除」按鈕；螢光筆模式支援 `Ctrl+Z` 撤銷最後一筆。

### Improved
- GUI 框架由 PyQt6 遷移至 PySide6（Qt 官方維護、LGPL 授權），功能與外觀不變。
- 內部架構重整：匯出、視窗狀態、更新流程拆分為獨立模組，並新增整合測試，行為不變。
- 刪除全部螢光標記後，不再殘留空的標記檔（.highlights.json）。

## [1.14.0] - 2026-07-09

### Added
- 新增偏好設定視窗（設定 → 偏好設定…）：外觀（主題、內容縮放）、匯出（PDF 頁面尺寸/方向）、行為（更新檢查、自訂 CSS）集中管理，沿用既有設定值。

### Improved
- 大檔效能：Markdown 渲染移至背景執行緒，開啟大型文件不再凍結介面；編輯模式即時預覽改為停止輸入後才重新渲染。
- 匯出品質：Word/PPT 表格內的粗體、斜體、行內程式碼格式現在會保留；圖片依實際尺寸輸出（小圖不再被放大）；遠端圖片下載加入逾時與總量保護，失敗時以占位文字呈現。

### Fixed
- 修正快速切換分頁時可能顯示錯誤文件內容的競態問題。
- 修正非同步渲染後捲動位置（切換分頁、存檔重載）無法還原的問題。

### Added
- 新增匯出 Word（.docx）功能：由 Markdown 直接產生原生可編輯的 Word 文件，支援標題、清單、粗斜體、表格、程式碼區塊與圖片；Mermaid 圖與數學式自動轉為圖片內嵌，中文正文使用微軟正黑體。
- 分頁可右鍵「移至新視窗」，把單一文件拆出成獨立閱讀器視窗。

### Improved
- 單一實例機制：再次開啟 .md 檔（雙擊或命令列）不再開出新視窗，改為在既有視窗新增分頁並帶到前景。
- 獨立（拆出）視窗關閉時不再覆蓋主視窗的分頁還原記錄。

## [1.12.0] - 2026-07-01

### Added
- 加入 Mermaid 視覺化流程圖建立器，可用按鈕新增起點、程序、決策、終點節點，並拖曳節點調整位置。
- 支援流程圖自動連線、手動連線、自動佈局、右側屬性面板，以及 Mermaid 原始碼與視覺畫布雙向同步。
- 新增甘特圖視覺化編輯器，可編輯區段、任務、日期、狀態與持續時間。
- 新增序列圖、類別圖、狀態圖、ER 圖的結構化視覺編輯器，支援表格化編輯並回寫 Mermaid 語法。
- Mermaid 工作區支援從 Markdown fenced block 選取圖表編輯，儲存時回寫原文件。

### Improved
- Mermaid 工作區加入深色/淺色主題切換，並改善深色模式下的可讀性。
- 流程圖畫布支援滑鼠滾輪縮放、空白處拖曳平移，以及 `Ctrl+0` 重設縮放。
- 強化 Mermaid 圖表模板、語法片段、即時預覽、錯誤提示，以及 SVG/PNG 複製與匯出流程。
