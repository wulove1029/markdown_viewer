# 升級實作紀錄

已授權：效能、順暢度與閱讀工作台介面實作。保留原有文件與編輯安全機制。

## 進行中
- 已核對現況：背景掃描、非同步 PDF、Markdown body 快取與情境格式工具列均已存在。
- 已新增 tools/benchmark_upgrade.py；正執行修改前的隔離元件基線。
- 本輪實作：啟動/IPC、快取與索引 I/O、閱讀版面及主工具列；再依實測處理文件樹與 PDF。
- 驗證尚未完成，不宣稱達成原計畫速度目標。

## 驗收清單
- [x] 修改前後元件量測
- [x] 開檔、快取、取消與資料安全相關測試
- [x] 完整回歸
- [x] 亮暗模式畫面與不同縮放檢查（offscreen）
- [x] 獨立代理驗收
- [x] 打包版本建置；已明列封裝開檔未驗證項目與步驟

## 第一批實作及驗證
- 入口延後 import：修改前 p50 498.75 ms、修改後 141.38 ms（10 次子程序；非首屏計時）。
- 標籤不變跳過寫入：p50 1.403 ms → 0.101 ms（30 次）。
- Markdown body 快取加入容量預算、LRU 與更完整失效條件；舊 worker 取得 parser lock 後重查取消狀態。
- 文件樹每約 6 ms 交還事件迴圈；保留完成後選取與捲動，PDF 尺寸快取限定 8 份／合計 50,000 頁且不儲存密碼。
- 主工具列改清楚模式選單，偏好設定新增行寬／行距；亮暗色彩同步。
- 第一輪完整 tests：1313 passed、72 skipped；新增效能回歸 7 passed。
- 初次直接 pytest 掃到專案既有無權限暫存目錄，改明確 tests 路徑後完整通過，未刪除既有目錄。
- offscreen 截圖須載入 Windows 字型、停用 GPU 才能正確顯示；亮暗模式已檢視。此不取代實體顯示器驗證。
- 大型 Markdown 冷轉換仍慢；5 MB 合成樣本前後約 22／20 秒，不能宣稱已解決。

## 第一批交付狀態
- 完整回歸最終 1322 passed、72 skipped，42.14 秒。
- 獨立查核找出的兩項回歸已修復：保留 source 游標／選取；磁碟 I/O 不持 parser lock。
- 合成 10,000 筆樹最長工作片段 102.80 → 9.25 ms，總時間 83.66 → 82.31 ms；detached subtree 避免逐批掛入引起重複排版。
- 合成 3,000 頁 PDF 重複 load p50 154.63 → 93.43 ms。
- 首頁真實 WebEngine 滑鼠導航通過；自訂 scheme 被 Chromium 阻擋，改成攔截保留 HTTPS 網址，未發外部導航。wikilink 仍在 QApplication 前註冊。
- 最終 PyInstaller COLLECT 成功；執行中舊版接走封裝測試，封裝開檔不列通過，工具已加程序守門；未關閉使用者程式。
- 完整交付說明與未完成原計畫項目見 `docs/2026-09-07-upgrade-delivery.md`。
