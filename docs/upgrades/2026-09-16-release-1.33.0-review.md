# 1.33.0 發布準備：獨立驗收

日期：2026-09-16。驗收者：fresh agent `/root/review_release_133`。

**結論：發布準備通過，未發現阻擋提交、推送或建立發布 tag 的問題。** 使用者已明確授權升級版號、提交並發布。此報告驗收本機發布資料；不代表尚未發生的 GitHub 建置、正式 Release 或安裝檔啟動已通過。

## 驗收條件與結果

| 條件 | 核對結果 |
| --- | --- |
| 依 `DEVELOPMENT.md` 第 6 節同步版號 | 通過：實際 Python import 的 `app.version.VERSION` 及讀回 `installer.iss` 的 `MyAppVersion` 均為 `1.33.0`。 |
| 本版摘要與更新紀錄同步 | 通過：`RELEASE_NOTES` 為本版 PDF 手形、閱讀右鍵功能及文字操作相容性三條；`CHANGELOG.md` 第一個版本章節為 `[1.33.0] - 2026-09-16`，只有一處此版本標題。 |
| 宣告的功能與本次產品 diff 相符 | 通過：核對 `app/pdf_view.py` 的手形／Space／中鍵平移、快照、選單與資訊；`app/window.py` 的搜尋、點擊頁面註記、狀態訊息與筆模式同步；`app/shortcuts.py` 的操作說明。更新紀錄如實說明快照取可見像素、頁面註記走 sidecar，以及尚未加入的 PDF 編輯、簽名、旋轉與列印。 |
| 既有測試與獨立驗收仍適用於發布程式 | 通過：三個產品功能檔案的 SHA256 逐一與 `2026-09-16-pdf-hand-tools-review.md` 核對一致，未在功能驗收後變更。原始日誌讀回確認完整測試 `1682 passed, 77 skipped`、獨立測試 `333 passed, 1 warning`，UI smoke JSON 為 passed 且來源 PDF 不變。77 項 skipped 不視為通過。 |
| 升版後更新流程回歸 | 通過：主代理提供並落檔的 `release-version-tests.txt` 讀回為 `229 passed in 20.84s`，涵蓋 updater、update flow 與 window integration；主代理報告 exit code 0。 |
| Python import 與資料 read-back | 通過：本驗收者以 Python 3.13.6 實跑 import `app.version`、`app.pdf_view`、`app.window`、`app.shortcuts` 及版號／hash／流程斷言，exit code 0。機器可讀證據見 `evidence-2026-09-16/release-independent-readback.json`。 |
| Tag 與 installer 發布流程 | 通過靜態核對：`v1.33.0` 符合 workflow 的 `v*.*.*` tag trigger；workflow 具有 `contents: write`，使用 Windows、Python 3.13，從 tag 同步版號後執行 PyInstaller 與 Inno Setup，再以 `softprops/action-gh-release@v2` 上傳 `installer_output/*.exe`。依 installer 設定，預期檔名為 `MarkdownViewer_Setup_v1.33.0.exe`，Release 內文來源為 `CHANGELOG.md`。相關檔案與工具路徑均存在。 |
| 提交內容基本檢查 | 通過：`git diff --check` 與 `git diff --cached --check` 實跑 exit code 0。未執行任何 git mutation，亦未修改產品程式。 |

## 功能檔案 SHA256

```text
app/pdf_view.py  C0203CDEBBFBD9A1DA1A4C606CC837FA7A0848ED7E28A65D1EC8E584A3905CFC
app/window.py    103999ABBA0127F7ACAFD77727B436FD2A8B1FCA72B6E981BBC8C7067D17E482
app/shortcuts.py 901B8DC3A6E3D63797228B2960473E017C7C40B4CE4D566A7C842DF088824DF0
```

## 正式發布後仍須核對

1. 遠端 `main` 與 `v1.33.0` 指向本次發布提交。
2. 該 tag 的 GitHub Actions run 成功。
3. GitHub Release 已發布且包含非空的 `MarkdownViewer_Setup_v1.33.0.exe`，下載資產可取得；如有伺服器 SHA256 digest，應比對下載內容。
4. 正式安裝檔的安裝／啟動與實體滑鼠、多螢幕 DPI 操作本次尚未驗證。若未實跑，交付時須如實保留此限制；手動步驟為關閉舊版後安裝新版本，開啟大圖 PDF、放大、右鍵選手形並兩軸拖曳，再確認 Space／中鍵、快照貼上及切回文字選取。

此後續項目由主代理持續執行與記錄，不能以本報告的「發布準備通過」取代遠端發布驗證。
