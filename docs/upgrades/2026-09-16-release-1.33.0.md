# 1.33.0 發布紀錄

日期：2026-09-16。使用者已明確授權升級版號、提交並發布。

## 發布範圍與流程

- 依實際專案 `E:\markdown_viewer\DEVELOPMENT.md` 第 6 節執行。使用者訊息的 `E:\markdown\_viewer\DEVELOPMENT.md` 不存在，已使用目前專案同名指南。
- 版本由 1.32.0 升為 1.33.0，內容為 PDF 手形拖曳與常用閱讀右鍵選單。
- 先同步 `app/version.py`、`installer.iss`、`CHANGELOG.md` 與 `RELEASE_NOTES`，驗證後提交推送 main，再推送 `v1.33.0` tag。
- GitHub Actions `release.yml` 依 tag 建置 Windows 安裝檔，發布至 GitHub Release。驗收須確認 workflow 成功、tag 指向本次提交，以及安裝檔存在且版本正確。

## 已完成

- 發布前 fetch：本機 main 與 origin/main 相同，基底 `b8b20aa`；遠端 latest 為 v1.32.0，尚無 v1.33.0。
- 已執行 `py -3 -X utf8 tools/bump_version.py 1.33.0`，同步程式與安裝檔版號；已更新更新紀錄與三條本版摘要。
- 功能完整測試：1682 passed、77 skipped，exit 0；獨立驗收：333 passed。證據見 `evidence-2026-09-16/pytest-full.txt`、`independent-tests.txt`。
- 已確認本次三個產品功能檔案 SHA256 與前輪獨立驗收一致，未在通過後追加功能變動。
- 版本一致性實跑：import `app.version` 並核對 `VERSION=1.33.0`、三條 `RELEASE_NOTES`、Inno Setup 版號與 changelog 標題，exit 0。
- 升版後更新與主視窗測試：`py -3 -X utf8 -m pytest tests/test_updater.py tests/test_update_flow.py tests/test_window_integration.py -q -p no:cacheprovider` → **229 passed，20.84 秒，exit 0**，輸出見 `evidence-2026-09-16/release-version-tests.txt`。
- 發布準備 fresh agent 驗收通過：Python 3.13 import、版號、摘要、changelog、功能 SHA256、tag/installer 流程與 staged diff 均正常。見 `2026-09-16-release-1.33.0-review.md` 與 `evidence-2026-09-16/release-independent-readback.json`。

## 待完成

- 提交、推送 main 與 v1.33.0 tag。
- 遠端 workflow、release 與安裝檔驗證。
