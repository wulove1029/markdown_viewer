# 啟動復原升級（2026-09-08）

## 已完成：設計與來源確認

- 啟動原先只讀上次正常關閉的分頁；CLI 開檔更直接略過分頁恢復。
- 採用非模態「待復原草稿」清單，集中顯示快照名稱、路徑、時間與預覽；選擇繼續時沿用既有比較與復原選擇。
- 稍後不刪除資料；提供可重新開啟的選單入口；另存副本不覆寫原始路徑，也不自動清除快照。
- 將涵蓋有效／重複／損壞快照、原檔刪除、稍後、CLI 開檔與不改動來源的隔離測試。
- window.py 整合由主代理負責：`_review_recovery_path` 與 `session_state.show_pending_recovery` 選單入口；本分工不修改 window.py。

## 已完成：實作與首輪測試

- 新增 `app/recovery_browser.py`：非模態清單含路徑／時間／內容預覽，列舉 canonical 有效快照並去重，保護原檔刪除與空草稿；已存在的即時編輯緩衝區不被舊快照取代。
- `session_state.restore_startup` 統一一般與 CLI 開檔後的草稿發現；一般 session 仍保留原有延遲分頁載入。
- 另存副本沿用原始編碼／換行與原子寫入，拒絕原路徑與 hardlink 別名；取消或失敗保留快照。
- 首輪 31 項測試發現 Enter 同時啟動清單與 QDialog 預設按鈕而重複呼叫，已取消清單視窗的預設按鈕語意；重新實跑為 **31 passed in 0.81s，exit 0**。
- 命令：`py -3 -X utf8 -m pytest tests/test_startup_recovery.py tests/test_recovery.py tests/test_recovery_dialog.py -q -p no:cacheprovider --basetemp tmp/upgrade-recovery-20260908-r2`。

## 驗證範圍

- window.py 整合後已驗證重試「稍後」、目前分頁恢復與空草稿保護。
- 新清單 offscreen 已實跑並檢視；真實殺程序後完整啟動與實體高 DPI 仍未驗。

## 整合進度與實際發現

- 加上 `test_session_state_edit_backends.py`、完整 `test_window_integration.py` 後，相關回歸為 **191 passed in 15.53s，exit 0**。
- `recovery-ui-probe.py` offscreen 實跑 exit 0；實際清單 820×540、minimumSizeHint 244×277、NonModal、2 份草稿；來源未改動、缺失原檔未被建立。已檢視 `evidence-2026-09-08/recovery-inbox.png`，中文、路徑、預覽與按鈕可見。
- 已新增 `tests/test_recovery_startup_integration.py` 實際 MainWindow／EditorView 整合測試。初次 4 過 1 失敗：Markdown 稍後後可重訪、live document／undo 不覆蓋、Markdown／TXT 原檔缺失的空草稿皆通過；TXT「稍後」卻先建立原檔的 live buffer，遮住未處理草稿。
- 已交主代理修正 pending 草稿時的 TXT 與 Markdown 編輯入口保護；新增測試要求 Markdown 再次選「稍後」不得建立新 buffer 或在切頁時清除既有快照。
- 跨代理覆核安排：可靠性代理覆核 recovery；本代理覆核 relocation／file_ops。此刻尚未宣告整體完成。

## 跨代理覆核後修正

- 可靠性代理發現副本的 `.tmp`／`.bak` 若是原檔 hardlink，原子寫入仍可能經旁路改動來源。已把這兩個實際寫入位置納入 samefile／路徑預檢，新增兩項回歸；副本本身的原路徑及 hardlink 禁止仍保留。
- 跨資料夾的 Markdown 副本現在使用共用 `rebase_markdown_links`，保留相對附件的原始目標；不支援的資源語法會保留原草稿並回報。
- Windows 極端合法 ISO 時間可能在本地時區轉換時產生 OSError／OverflowError，已退回原時間字串並新增實際建立清單的兩項測試。
- 最新 `tests/test_startup_recovery.py`：**19 passed in 0.75s，exit 0**，使用 `--basetemp tmp/upgrade-recovery-timestamps-r6`。

## 最終實作驗證

- 主代理的 pending 草稿保護已整合。`test_recovery_startup_integration.py` 為 **6 passed in 0.89s，exit 0**：包含 Markdown／TXT 稍後後重訪同一分頁、live document 與 undo 保留、兩種文字格式的空且原檔缺失草稿，以及 Markdown 再次稍後與切頁不丟草稿。
- 最新合併驗證：`py -3 -X utf8 -m pytest tests/test_startup_recovery.py tests/test_recovery_startup_integration.py tests/test_recovery.py tests/test_recovery_dialog.py tests/test_editor_data_safety.py -q -p no:cacheprovider --basetemp tmp/upgrade-recovery-final-r7` → **99 passed in 10.11s，exit 0**。
- 匯入檢查 `import main; import app.session_state; import app.recovery_browser` exit 0。
- 本分工產品修改僅 `app/recovery_browser.py`、`app/session_state.py`、`main.py`；window.py 是主代理整合。所有新測試使用隔離 QSettings、RecoveryStore、臨時來源檔，未使用個人文件做故障注入。
- 待可靠性代理完成最終獨立覆核；本紀錄的前段失敗為修正過程，不是目前殘留失敗。

尚未實機驗證的補測步驟：另建測試筆記→輸入草稿→等待至少 1 秒→從工作管理員結束測試中的程式→以另一個檔案啟動→確認「待復原草稿」可列出並預覽→選稍後後由檔案選單重新開啟→復原或另存副本→確認原檔未被自動覆寫。請先使用測試文件，勿用唯一正式文件做終止程序測試。
