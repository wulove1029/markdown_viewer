# 1.33.1 發布獨立驗收

日期：2026-09-16。驗收者：fresh agent `review_release_1331`。

依全域 `AGENTS.md` 引用的 `03-judgment.md` R2，獨立檢查版本、發布範圍、測試證據與自動發布流程。使用者已確認本機修正版可用，並明確授權升版、提交與發布。本次驗收未操作使用者的執行中程式，未建立 MainWindow 或原生 QSettings，也未修改產品檔案、提交或推送。

## 發布前結論

發布前驗收通過，未發現阻擋發布的產品或版本問題。正式發布後的遠端 tag、workflow、Release 與安裝檔驗證需完成下方讀回紀錄，才能宣告發布完成。

| 項目 | 獨立核對結果 |
| --- | --- |
| 開發規範 | 已閱讀專案 `DEVELOPMENT.md` 第 6 節；先同步程式及安裝版號、CHANGELOG、本版摘要，再提交推送與推送 tag。 |
| 版號 | `app/version.py` 的 VERSION 與 `installer.iss` 的 MyAppVersion 均為 `1.33.1`；CHANGELOG 最新已發布條目日期為 2026-09-16。 |
| 使用者摘要 | RELEASE_NOTES 三條均是本版工作階段還原、分頁自動保存及 PDF 頁碼修正，與 CHANGELOG 和產品 diff 一致，沒有沿用 1.33.0 摘要。 |
| 發布範圍 | 產品修改集中於工作階段保存／還原、PDF 第一頁與多文件頁碼切換，以及啟動路由；另含必要測試、隔離清理、驗證工具與交付證據。 |
| 完整回歸 | 已直接讀回 `full-regression.txt`：1696 passed、77 skipped；原生工作階段前後一致。 |
| 真實重啟 | 已解析 `session-restart-smoke.json`：五個獨立程序均通過，包含無 closeEvent 結束及再次開啟；四份來源 PDF 雜湊不變，設定隔離成立。 |
| 獨立功能驗收 | 已閱讀 `2026-09-16-session-restore-review.md`，包含測試隔離失誤更正，以及修正後的 182 passed 與計時器生命週期補驗。 |
| 升版後測試 | 已讀回 `release-version-tests.txt` 的 230 passed，並檢查 runner 在應用程式匯入前強制使用明確 INI；本次升版測試沒有建立原生設定。 |
| 觸發與封裝 | `.github/workflows/release.yml` 由 `v*.*.*` tag 觸發，使用 Windows／Python 3.13，以 tag 同步版號後透過 PyInstaller／Inno Setup 建置，發布 `installer_output/*.exe`。 |
| 更新相容性 | 本次沒有更改既有 repo、Release 命名規則、安裝檔命名或 updater 格式；版本是 1.33.0 後的修正版 1.33.1。 |

獨立純靜態／metadata 斷言實跑 exit code 0：

```text
Independent release preflight: passed (version, notes, syntax, evidence, workflow); no application/QSettings instantiated
```

此檢查包含匯入純常數 `app.version`、核對 VERSION／installer／CHANGELOG、解析本次產品與驗證工具的 Python 語法、解析五程序 JSON、核對測試摘要及 workflow 關鍵設定。`git diff --check` 同樣 exit code 0。

初讀證據時發現 `build.txt`、`full-regression.txt`、`release-version-tests.txt` 為 PowerShell 輸出的 UTF-16 BOM，主代理已在提交前轉成 UTF-8。獨立重讀三檔均可用 UTF-8 解碼；同時核對 `release-version-check.json` 的 exit code 0 與四個產品檔 SHA-256 均符合目前工作樹。內容解析後測試結果一致，這是證據文字編碼問題，並非功能測試失敗。最後 `git diff --cached --check` exit code 0。

## 發布後讀回

待主代理提交、推送 tag 並完成 GitHub Actions 後補記。尚未將本機測試版視為正式 1.33.1 安裝檔，也不宣稱已執行正式安裝。
